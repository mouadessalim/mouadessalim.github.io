#!/bin/sh
# Automatically bump patch version in package.json on every commit
npm version patch --no-git-tag-version

# Re-stage the updated package.json and package-lock.json
git add package.json
if [ -f package-lock.json ]; then
  git add package-lock.json
fi
